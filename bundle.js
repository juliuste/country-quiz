(function e(t,n,r){function s(o,u){if(!n[o]){if(!t[o]){var a=typeof require=="function"&&require;if(!u&&a)return a(o,!0);if(i)return i(o,!0);var f=new Error("Cannot find module '"+o+"'");throw f.code="MODULE_NOT_FOUND",f}var l=n[o]={exports:{}};t[o][0].call(l.exports,function(e){var n=t[o][1][e];return s(n?n:e)},l,l.exports,e,t,n,r)}return n[o].exports}var i=typeof require=="function"&&require;for(var o=0;o<r.length;o++)s(r[o]);return s})({1:[function(require,module,exports){
module.exports=[{"capital":"Kabul","population":27657145,"area":652230,"flag":"https://restcountries.eu/data/afg.svg","code":"AF","name":"Afghanistan","density":42.4},{"capital":"Mariehamn","population":28875,"area":1580,"flag":"https://restcountries.eu/data/ala.svg","code":"AX","name":"Åland Islands","density":18.3},{"capital":"Tirana","population":2886026,"area":28748,"flag":"https://restcountries.eu/data/alb.svg","code":"AL","name":"Albania","density":100.4},{"capital":"Algiers","population":40400000,"area":2381741,"flag":"https://restcountries.eu/data/dza.svg","code":"DZ","name":"Algeria","density":17},{"capital":"Pago Pago","population":57100,"area":199,"flag":"https://restcountries.eu/data/asm.svg","code":"AS","name":"American Samoa","density":286.9},{"capital":"Andorra la Vella","population":78014,"area":468,"flag":"https://restcountries.eu/data/and.svg","code":"AD","name":"Andorra","density":166.7},{"capital":"Luanda","population":25868000,"area":1246700,"flag":"https://restcountries.eu/data/ago.svg","code":"AO","name":"Angola","density":20.7},{"capital":"The Valley","population":13452,"area":91,"flag":"https://restcountries.eu/data/aia.svg","code":"AI","name":"Anguilla","density":147.8},{"capital":"Buenos Aires","population":43590400,"area":2780400,"flag":"https://restcountries.eu/data/arg.svg","code":"AR","name":"Argentina","density":15.7},{"capital":"Yerevan","population":2994400,"area":29743,"flag":"https://restcountries.eu/data/arm.svg","code":"AM","name":"Armenia","density":100.7},{"capital":"Oranjestad","population":107394,"area":180,"flag":"https://restcountries.eu/data/abw.svg","code":"AW","name":"Aruba","density":596.6},{"capital":"Canberra","population":24117360,"area":7692024,"flag":"https://restcountries.eu/data/aus.svg","code":"AU","name":"Australia","density":3.1},{"capital":"Vienna","population":8725931,"area":83871,"flag":"https://restcountries.eu/data/aut.svg","code":"AT","name":"Austria","density":104},{"capital":"Baku","population":9730500,"area":86600,"flag":"https://restcountries.eu/data/aze.svg","code":"AZ","name":"Azerbaijan","density":112.4},{"capital":"Nassau","population":378040,"area":13943,"flag":"https://restcountries.eu/data/bhs.svg","code":"BS","name":"Bahamas","density":27.1},{"capital":"Manama","population":1404900,"area":765,"flag":"https://restcountries.eu/data/bhr.svg","code":"BH","name":"Bahrain","density":1836.5},{"capital":"Dhaka","population":161006790,"area":147570,"flag":"https://restcountries.eu/data/bgd.svg","code":"BD","name":"Bangladesh","density":1091.1},{"capital":"Bridgetown","population":285000,"area":430,"flag":"https://restcountries.eu/data/brb.svg","code":"BB","name":"Barbados","density":662.8},{"capital":"Minsk","population":9498700,"area":207600,"flag":"https://restcountries.eu/data/blr.svg","code":"BY","name":"Belarus","density":45.8},{"capital":"Brussels","population":11319511,"area":30528,"flag":"https://restcountries.eu/data/bel.svg","code":"BE","name":"Belgium","density":370.8},{"capital":"Belmopan","population":370300,"area":22966,"flag":"https://restcountries.eu/data/blz.svg","code":"BZ","name":"Belize","density":16.1},{"capital":"Porto-Novo","population":10653654,"area":112622,"flag":"https://restcountries.eu/data/ben.svg","code":"BJ","name":"Benin","density":94.6},{"capital":"Hamilton","population":61954,"area":54,"flag":"https://restcountries.eu/data/bmu.svg","code":"BM","name":"Bermuda","density":1147.3},{"capital":"Thimphu","population":775620,"area":38394,"flag":"https://restcountries.eu/data/btn.svg","code":"BT","name":"Bhutan","density":20.2},{"capital":"Gaborone","population":2141206,"area":582000,"flag":"https://restcountries.eu/data/bwa.svg","code":"BW","name":"Botswana","density":3.7},{"capital":"Brasília","population":206135893,"area":8515767,"flag":"https://restcountries.eu/data/bra.svg","code":"BR","name":"Brazil","density":24.2},{"capital":"Sofia","population":7153784,"area":110879,"flag":"https://restcountries.eu/data/bgr.svg","code":"BG","name":"Bulgaria","density":64.5},{"capital":"Ouagadougou","population":19034397,"area":272967,"flag":"https://restcountries.eu/data/bfa.svg","code":"BF","name":"Burkina Faso","density":69.7},{"capital":"Bujumbura","population":10114505,"area":27834,"flag":"https://restcountries.eu/data/bdi.svg","code":"BI","name":"Burundi","density":363.4},{"capital":"Phnom Penh","population":15626444,"area":181035,"flag":"https://restcountries.eu/data/khm.svg","code":"KH","name":"Cambodia","density":86.3},{"capital":"Yaoundé","population":22709892,"area":475442,"flag":"https://restcountries.eu/data/cmr.svg","code":"CM","name":"Cameroon","density":47.8},{"capital":"Ottawa","population":36155487,"area":9984670,"flag":"https://restcountries.eu/data/can.svg","code":"CA","name":"Canada","density":3.6},{"capital":"Praia","population":531239,"area":4033,"flag":"https://restcountries.eu/data/cpv.svg","code":"CV","name":"Cabo Verde","density":131.7},{"capital":"George Town","population":58238,"area":264,"flag":"https://restcountries.eu/data/cym.svg","code":"KY","name":"Cayman Islands","density":220.6},{"capital":"N'Djamena","population":14497000,"area":1284000,"flag":"https://restcountries.eu/data/tcd.svg","code":"TD","name":"Chad","density":11.3},{"capital":"Santiago","population":18191900,"area":756102,"flag":"https://restcountries.eu/data/chl.svg","code":"CL","name":"Chile","density":24.1},{"capital":"Beijing","population":1377422166,"area":9640011,"flag":"https://restcountries.eu/data/chn.svg","code":"CN","name":"China","density":142.9},{"capital":"Bogotá","population":48759958,"area":1141748,"flag":"https://restcountries.eu/data/col.svg","code":"CO","name":"Colombia","density":42.7},{"capital":"Moroni","population":806153,"area":1862,"flag":"https://restcountries.eu/data/com.svg","code":"KM","name":"Comoros","density":433},{"capital":"Brazzaville","population":4741000,"area":342000,"flag":"https://restcountries.eu/data/cog.svg","code":"CG","name":"Congo","density":13.9},{"capital":"Avarua","population":18100,"area":236,"flag":"https://restcountries.eu/data/cok.svg","code":"CK","name":"Cook Islands","density":76.7},{"capital":"San José","population":4890379,"area":51100,"flag":"https://restcountries.eu/data/cri.svg","code":"CR","name":"Costa Rica","density":95.7},{"capital":"Zagreb","population":4190669,"area":56594,"flag":"https://restcountries.eu/data/hrv.svg","code":"HR","name":"Croatia","density":74},{"capital":"Havana","population":11239004,"area":109884,"flag":"https://restcountries.eu/data/cub.svg","code":"CU","name":"Cuba","density":102.3},{"capital":"Willemstad","population":154843,"area":444,"flag":"https://restcountries.eu/data/cuw.svg","code":"CW","name":"Curaçao","density":348.7},{"capital":"Nicosia","population":847000,"area":9251,"flag":"https://restcountries.eu/data/cyp.svg","code":"CY","name":"Cyprus","density":91.6},{"capital":"Prague","population":10558524,"area":78865,"flag":"https://restcountries.eu/data/cze.svg","code":"CZ","name":"Czech Republic","density":133.9},{"capital":"Copenhagen","population":5717014,"area":43094,"flag":"https://restcountries.eu/data/dnk.svg","code":"DK","name":"Denmark","density":132.7},{"capital":"Djibouti","population":900000,"area":23200,"flag":"https://restcountries.eu/data/dji.svg","code":"DJ","name":"Djibouti","density":38.8},{"capital":"Roseau","population":71293,"area":751,"flag":"https://restcountries.eu/data/dma.svg","code":"DM","name":"Dominica","density":94.9},{"capital":"Quito","population":16545799,"area":276841,"flag":"https://restcountries.eu/data/ecu.svg","code":"EC","name":"Ecuador","density":59.8},{"capital":"Cairo","population":91290000,"area":1002450,"flag":"https://restcountries.eu/data/egy.svg","code":"EG","name":"Egypt","density":91.1},{"capital":"San Salvador","population":6520675,"area":21041,"flag":"https://restcountries.eu/data/slv.svg","code":"SV","name":"El Salvador","density":309.9},{"capital":"Asmara","population":5352000,"area":117600,"flag":"https://restcountries.eu/data/eri.svg","code":"ER","name":"Eritrea","density":45.5},{"capital":"Tallinn","population":1315944,"area":45227,"flag":"https://restcountries.eu/data/est.svg","code":"EE","name":"Estonia","density":29.1},{"capital":"Addis Ababa","population":92206005,"area":1104300,"flag":"https://restcountries.eu/data/eth.svg","code":"ET","name":"Ethiopia","density":83.5},{"capital":"Tórshavn","population":49376,"area":1393,"flag":"https://restcountries.eu/data/fro.svg","code":"FO","name":"Faroe Islands","density":35.4},{"capital":"Suva","population":867000,"area":18272,"flag":"https://restcountries.eu/data/fji.svg","code":"FJ","name":"Fiji","density":47.4},{"capital":"Helsinki","population":5491817,"area":338424,"flag":"https://restcountries.eu/data/fin.svg","code":"FI","name":"Finland","density":16.2},{"capital":"Paris","population":66710000,"area":640679,"flag":"https://restcountries.eu/data/fra.svg","code":"FR","name":"France","density":104.1},{"capital":"Libreville","population":1802278,"area":267668,"flag":"https://restcountries.eu/data/gab.svg","code":"GA","name":"Gabon","density":6.7},{"capital":"Banjul","population":1882450,"area":11295,"flag":"https://restcountries.eu/data/gmb.svg","code":"GM","name":"Gambia","density":166.7},{"capital":"Tbilisi","population":3720400,"area":69700,"flag":"https://restcountries.eu/data/geo.svg","code":"GE","name":"Georgia","density":53.4},{"capital":"Berlin","population":81770900,"area":357114,"flag":"https://restcountries.eu/data/deu.svg","code":"DE","name":"Germany","density":229},{"capital":"Accra","population":27670174,"area":238533,"flag":"https://restcountries.eu/data/gha.svg","code":"GH","name":"Ghana","density":116},{"capital":"Gibraltar","population":33140,"area":6,"flag":"https://restcountries.eu/data/gib.svg","code":"GI","name":"Gibraltar","density":5523.3},{"capital":"Athens","population":10858018,"area":131990,"flag":"https://restcountries.eu/data/grc.svg","code":"GR","name":"Greece","density":82.3},{"capital":"St. George's","population":103328,"area":344,"flag":"https://restcountries.eu/data/grd.svg","code":"GD","name":"Grenada","density":300.4},{"capital":"Hagåtña","population":184200,"area":549,"flag":"https://restcountries.eu/data/gum.svg","code":"GU","name":"Guam","density":335.5},{"capital":"Guatemala City","population":16176133,"area":108889,"flag":"https://restcountries.eu/data/gtm.svg","code":"GT","name":"Guatemala","density":148.6},{"capital":"St. Peter Port","population":62999,"area":78,"flag":"https://restcountries.eu/data/ggy.svg","code":"GG","name":"Guernsey","density":807.7},{"capital":"Conakry","population":12947000,"area":245857,"flag":"https://restcountries.eu/data/gin.svg","code":"GN","name":"Guinea","density":52.7},{"capital":"Bissau","population":1547777,"area":36125,"flag":"https://restcountries.eu/data/gnb.svg","code":"GW","name":"Guinea-Bissau","density":42.8},{"capital":"Georgetown","population":746900,"area":214969,"flag":"https://restcountries.eu/data/guy.svg","code":"GY","name":"Guyana","density":3.5},{"capital":"Port-au-Prince","population":11078033,"area":27750,"flag":"https://restcountries.eu/data/hti.svg","code":"HT","name":"Haiti","density":399.2},{"capital":"Rome","population":451,"area":0.44,"flag":"https://restcountries.eu/data/vat.svg","code":"VA","name":"Holy See","density":1025},{"capital":"Tegucigalpa","population":8576532,"area":112492,"flag":"https://restcountries.eu/data/hnd.svg","code":"HN","name":"Honduras","density":76.2},{"capital":"City of Victoria","population":7324300,"area":1104,"flag":"https://restcountries.eu/data/hkg.svg","code":"HK","name":"Hong Kong","density":6634.3},{"capital":"Budapest","population":9823000,"area":93028,"flag":"https://restcountries.eu/data/hun.svg","code":"HU","name":"Hungary","density":105.6},{"capital":"Reykjavík","population":334300,"area":103000,"flag":"https://restcountries.eu/data/isl.svg","code":"IS","name":"Iceland","density":3.2},{"capital":"New Delhi","population":1295210000,"area":3287590,"flag":"https://restcountries.eu/data/ind.svg","code":"IN","name":"India","density":394},{"capital":"Jakarta","population":258705000,"area":1904569,"flag":"https://restcountries.eu/data/idn.svg","code":"ID","name":"Indonesia","density":135.8},{"capital":"Yamoussoukro","population":22671331,"area":322463,"flag":"https://restcountries.eu/data/civ.svg","code":"CI","name":"Côte d'Ivoire","density":70.3},{"capital":"Baghdad","population":37883543,"area":438317,"flag":"https://restcountries.eu/data/irq.svg","code":"IQ","name":"Iraq","density":86.4},{"capital":"Dublin","population":6378000,"area":70273,"flag":"https://restcountries.eu/data/irl.svg","code":"IE","name":"Ireland","density":90.8},{"capital":"Douglas","population":84497,"area":572,"flag":"https://restcountries.eu/data/imn.svg","code":"IM","name":"Isle of Man","density":147.7},{"capital":"Jerusalem","population":8527400,"area":20770,"flag":"https://restcountries.eu/data/isr.svg","code":"IL","name":"Israel","density":410.6},{"capital":"Rome","population":60665551,"area":301336,"flag":"https://restcountries.eu/data/ita.svg","code":"IT","name":"Italy","density":201.3},{"capital":"Kingston","population":2723246,"area":10991,"flag":"https://restcountries.eu/data/jam.svg","code":"JM","name":"Jamaica","density":247.8},{"capital":"Tokyo","population":126960000,"area":377930,"flag":"https://restcountries.eu/data/jpn.svg","code":"JP","name":"Japan","density":335.9},{"capital":"Saint Helier","population":100800,"area":116,"flag":"https://restcountries.eu/data/jey.svg","code":"JE","name":"Jersey","density":869},{"capital":"Amman","population":9531712,"area":89342,"flag":"https://restcountries.eu/data/jor.svg","code":"JO","name":"Jordan","density":106.7},{"capital":"Astana","population":17753200,"area":2724900,"flag":"https://restcountries.eu/data/kaz.svg","code":"KZ","name":"Kazakhstan","density":6.5},{"capital":"Nairobi","population":47251000,"area":580367,"flag":"https://restcountries.eu/data/ken.svg","code":"KE","name":"Kenya","density":81.4},{"capital":"South Tarawa","population":113400,"area":811,"flag":"https://restcountries.eu/data/kir.svg","code":"KI","name":"Kiribati","density":139.8},{"capital":"Kuwait City","population":4183658,"area":17818,"flag":"https://restcountries.eu/data/kwt.svg","code":"KW","name":"Kuwait","density":234.8},{"capital":"Bishkek","population":6047800,"area":199951,"flag":"https://restcountries.eu/data/kgz.svg","code":"KG","name":"Kyrgyzstan","density":30.2},{"capital":"Riga","population":1961600,"area":64559,"flag":"https://restcountries.eu/data/lva.svg","code":"LV","name":"Latvia","density":30.4},{"capital":"Beirut","population":5988000,"area":10452,"flag":"https://restcountries.eu/data/lbn.svg","code":"LB","name":"Lebanon","density":572.9},{"capital":"Maseru","population":1894194,"area":30355,"flag":"https://restcountries.eu/data/lso.svg","code":"LS","name":"Lesotho","density":62.4},{"capital":"Monrovia","population":4615000,"area":111369,"flag":"https://restcountries.eu/data/lbr.svg","code":"LR","name":"Liberia","density":41.4},{"capital":"Tripoli","population":6385000,"area":1759540,"flag":"https://restcountries.eu/data/lby.svg","code":"LY","name":"Libya","density":3.6},{"capital":"Vaduz","population":37623,"area":160,"flag":"https://restcountries.eu/data/lie.svg","code":"LI","name":"Liechtenstein","density":235.1},{"capital":"Vilnius","population":2872294,"area":65300,"flag":"https://restcountries.eu/data/ltu.svg","code":"LT","name":"Lithuania","density":44},{"capital":"Luxembourg","population":576200,"area":2586,"flag":"https://restcountries.eu/data/lux.svg","code":"LU","name":"Luxembourg","density":222.8},{"capital":"","population":649100,"area":30,"flag":"https://restcountries.eu/data/mac.svg","code":"MO","name":"Macao","density":21636.7},{"capital":"Antananarivo","population":22434363,"area":587041,"flag":"https://restcountries.eu/data/mdg.svg","code":"MG","name":"Madagascar","density":38.2},{"capital":"Lilongwe","population":16832910,"area":118484,"flag":"https://restcountries.eu/data/mwi.svg","code":"MW","name":"Malawi","density":142.1},{"capital":"Kuala Lumpur","population":31405416,"area":330803,"flag":"https://restcountries.eu/data/mys.svg","code":"MY","name":"Malaysia","density":94.9},{"capital":"Malé","population":344023,"area":300,"flag":"https://restcountries.eu/data/mdv.svg","code":"MV","name":"Maldives","density":1146.7},{"capital":"Bamako","population":18135000,"area":1240192,"flag":"https://restcountries.eu/data/mli.svg","code":"ML","name":"Mali","density":14.6},{"capital":"Valletta","population":425384,"area":316,"flag":"https://restcountries.eu/data/mlt.svg","code":"MT","name":"Malta","density":1346.2},{"capital":"Nouakchott","population":3718678,"area":1030700,"flag":"https://restcountries.eu/data/mrt.svg","code":"MR","name":"Mauritania","density":3.6},{"capital":"Port Louis","population":1262879,"area":2040,"flag":"https://restcountries.eu/data/mus.svg","code":"MU","name":"Mauritius","density":619.1},{"capital":"Mexico City","population":122273473,"area":1964375,"flag":"https://restcountries.eu/data/mex.svg","code":"MX","name":"Mexico","density":62.2},{"capital":"Monaco","population":38400,"area":2.02,"flag":"https://restcountries.eu/data/mco.svg","code":"MC","name":"Monaco","density":19009.9},{"capital":"Ulan Bator","population":3093100,"area":1564110,"flag":"https://restcountries.eu/data/mng.svg","code":"MN","name":"Mongolia","density":2},{"capital":"Podgorica","population":621810,"area":13812,"flag":"https://restcountries.eu/data/mne.svg","code":"ME","name":"Montenegro","density":45},{"capital":"Plymouth","population":4922,"area":102,"flag":"https://restcountries.eu/data/msr.svg","code":"MS","name":"Montserrat","density":48.3},{"capital":"Rabat","population":33337529,"area":446550,"flag":"https://restcountries.eu/data/mar.svg","code":"MA","name":"Morocco","density":74.7},{"capital":"Maputo","population":26423700,"area":801590,"flag":"https://restcountries.eu/data/moz.svg","code":"MZ","name":"Mozambique","density":33},{"capital":"Naypyidaw","population":51419420,"area":676578,"flag":"https://restcountries.eu/data/mmr.svg","code":"MM","name":"Myanmar","density":76},{"capital":"Windhoek","population":2324388,"area":825615,"flag":"https://restcountries.eu/data/nam.svg","code":"NA","name":"Namibia","density":2.8},{"capital":"Yaren","population":10084,"area":21,"flag":"https://restcountries.eu/data/nru.svg","code":"NR","name":"Nauru","density":480.2},{"capital":"Kathmandu","population":28431500,"area":147181,"flag":"https://restcountries.eu/data/npl.svg","code":"NP","name":"Nepal","density":193.2},{"capital":"Amsterdam","population":17019800,"area":41850,"flag":"https://restcountries.eu/data/nld.svg","code":"NL","name":"Netherlands","density":406.7},{"capital":"Nouméa","population":268767,"area":18575,"flag":"https://restcountries.eu/data/ncl.svg","code":"NC","name":"New Caledonia","density":14.5},{"capital":"Wellington","population":4697854,"area":270467,"flag":"https://restcountries.eu/data/nzl.svg","code":"NZ","name":"New Zealand","density":17.4},{"capital":"Managua","population":6262703,"area":130373,"flag":"https://restcountries.eu/data/nic.svg","code":"NI","name":"Nicaragua","density":48},{"capital":"Niamey","population":20715000,"area":1267000,"flag":"https://restcountries.eu/data/ner.svg","code":"NE","name":"Niger","density":16.3},{"capital":"Abuja","population":186988000,"area":923768,"flag":"https://restcountries.eu/data/nga.svg","code":"NG","name":"Nigeria","density":202.4},{"capital":"Alofi","population":1470,"area":260,"flag":"https://restcountries.eu/data/niu.svg","code":"NU","name":"Niue","density":5.7},{"capital":"Kingston","population":2302,"area":36,"flag":"https://restcountries.eu/data/nfk.svg","code":"NF","name":"Norfolk Island","density":63.9},{"capital":"Oslo","population":5223256,"area":323802,"flag":"https://restcountries.eu/data/nor.svg","code":"NO","name":"Norway","density":16.1},{"capital":"Muscat","population":4420133,"area":309500,"flag":"https://restcountries.eu/data/omn.svg","code":"OM","name":"Oman","density":14.3},{"capital":"Islamabad","population":194125062,"area":881912,"flag":"https://restcountries.eu/data/pak.svg","code":"PK","name":"Pakistan","density":220.1},{"capital":"Ngerulmud","population":17950,"area":459,"flag":"https://restcountries.eu/data/plw.svg","code":"PW","name":"Palau","density":39.1},{"capital":"Panama City","population":3814672,"area":75417,"flag":"https://restcountries.eu/data/pan.svg","code":"PA","name":"Panama","density":50.6},{"capital":"Asunción","population":6854536,"area":406752,"flag":"https://restcountries.eu/data/pry.svg","code":"PY","name":"Paraguay","density":16.9},{"capital":"Lima","population":31488700,"area":1285216,"flag":"https://restcountries.eu/data/per.svg","code":"PE","name":"Peru","density":24.5},{"capital":"Manila","population":103279800,"area":342353,"flag":"https://restcountries.eu/data/phl.svg","code":"PH","name":"Philippines","density":301.7},{"capital":"Adamstown","population":56,"area":47,"flag":"https://restcountries.eu/data/pcn.svg","code":"PN","name":"Pitcairn","density":1.2},{"capital":"Warsaw","population":38437239,"area":312679,"flag":"https://restcountries.eu/data/pol.svg","code":"PL","name":"Poland","density":122.9},{"capital":"Lisbon","population":10374822,"area":92090,"flag":"https://restcountries.eu/data/prt.svg","code":"PT","name":"Portugal","density":112.7},{"capital":"San Juan","population":3474182,"area":8870,"flag":"https://restcountries.eu/data/pri.svg","code":"PR","name":"Puerto Rico","density":391.7},{"capital":"Doha","population":2587564,"area":11586,"flag":"https://restcountries.eu/data/qat.svg","code":"QA","name":"Qatar","density":223.3},{"capital":"Bucharest","population":19861408,"area":238391,"flag":"https://restcountries.eu/data/rou.svg","code":"RO","name":"Romania","density":83.3},{"capital":"Kigali","population":11553188,"area":26338,"flag":"https://restcountries.eu/data/rwa.svg","code":"RW","name":"Rwanda","density":438.7},{"capital":"Castries","population":186000,"area":616,"flag":"https://restcountries.eu/data/lca.svg","code":"LC","name":"Saint Lucia","density":301.9},{"capital":"Apia","population":194899,"area":2842,"flag":"https://restcountries.eu/data/wsm.svg","code":"WS","name":"Samoa","density":68.6},{"capital":"City of San Marino","population":33005,"area":61,"flag":"https://restcountries.eu/data/smr.svg","code":"SM","name":"San Marino","density":541.1},{"capital":"Riyadh","population":32248200,"area":2149690,"flag":"https://restcountries.eu/data/sau.svg","code":"SA","name":"Saudi Arabia","density":15},{"capital":"Dakar","population":14799859,"area":196722,"flag":"https://restcountries.eu/data/sen.svg","code":"SN","name":"Senegal","density":75.2},{"capital":"Belgrade","population":7076372,"area":88361,"flag":"https://restcountries.eu/data/srb.svg","code":"RS","name":"Serbia","density":80.1},{"capital":"Victoria","population":91400,"area":452,"flag":"https://restcountries.eu/data/syc.svg","code":"SC","name":"Seychelles","density":202.2},{"capital":"Freetown","population":7075641,"area":71740,"flag":"https://restcountries.eu/data/sle.svg","code":"SL","name":"Sierra Leone","density":98.6},{"capital":"Singapore","population":5535000,"area":710,"flag":"https://restcountries.eu/data/sgp.svg","code":"SG","name":"Singapore","density":7795.8},{"capital":"Bratislava","population":5426252,"area":49037,"flag":"https://restcountries.eu/data/svk.svg","code":"SK","name":"Slovakia","density":110.7},{"capital":"Ljubljana","population":2064188,"area":20273,"flag":"https://restcountries.eu/data/svn.svg","code":"SI","name":"Slovenia","density":101.8},{"capital":"Honiara","population":642000,"area":28896,"flag":"https://restcountries.eu/data/slb.svg","code":"SB","name":"Solomon Islands","density":22.2},{"capital":"Mogadishu","population":11079000,"area":637657,"flag":"https://restcountries.eu/data/som.svg","code":"SO","name":"Somalia","density":17.4},{"capital":"Pretoria","population":55653654,"area":1221037,"flag":"https://restcountries.eu/data/zaf.svg","code":"ZA","name":"South Africa","density":45.6},{"capital":"Juba","population":12131000,"area":619745,"flag":"https://restcountries.eu/data/ssd.svg","code":"SS","name":"South Sudan","density":19.6},{"capital":"Madrid","population":46438422,"area":505992,"flag":"https://restcountries.eu/data/esp.svg","code":"ES","name":"Spain","density":91.8},{"capital":"Colombo","population":20966000,"area":65610,"flag":"https://restcountries.eu/data/lka.svg","code":"LK","name":"Sri Lanka","density":319.6},{"capital":"Khartoum","population":39598700,"area":1886068,"flag":"https://restcountries.eu/data/sdn.svg","code":"SD","name":"Sudan","density":21},{"capital":"Paramaribo","population":541638,"area":163820,"flag":"https://restcountries.eu/data/sur.svg","code":"SR","name":"Suriname","density":3.3},{"capital":"Lobamba","population":1132657,"area":17364,"flag":"https://restcountries.eu/data/swz.svg","code":"SZ","name":"Swaziland","density":65.2},{"capital":"Stockholm","population":9894888,"area":450295,"flag":"https://restcountries.eu/data/swe.svg","code":"SE","name":"Sweden","density":22},{"capital":"Bern","population":8341600,"area":41284,"flag":"https://restcountries.eu/data/che.svg","code":"CH","name":"Switzerland","density":202.1},{"capital":"Dushanbe","population":8593600,"area":143100,"flag":"https://restcountries.eu/data/tjk.svg","code":"TJ","name":"Tajikistan","density":60.1},{"capital":"Bangkok","population":65327652,"area":513120,"flag":"https://restcountries.eu/data/tha.svg","code":"TH","name":"Thailand","density":127.3},{"capital":"Dili","population":1167242,"area":14874,"flag":"https://restcountries.eu/data/tls.svg","code":"TL","name":"Timor-Leste","density":78.5},{"capital":"Lomé","population":7143000,"area":56785,"flag":"https://restcountries.eu/data/tgo.svg","code":"TG","name":"Togo","density":125.8},{"capital":"Fakaofo","population":1411,"area":12,"flag":"https://restcountries.eu/data/tkl.svg","code":"TK","name":"Tokelau","density":117.6},{"capital":"Nuku'alofa","population":103252,"area":747,"flag":"https://restcountries.eu/data/ton.svg","code":"TO","name":"Tonga","density":138.2},{"capital":"Tunis","population":11154400,"area":163610,"flag":"https://restcountries.eu/data/tun.svg","code":"TN","name":"Tunisia","density":68.2},{"capital":"Ankara","population":78741053,"area":783562,"flag":"https://restcountries.eu/data/tur.svg","code":"TR","name":"Turkey","density":100.5},{"capital":"Ashgabat","population":4751120,"area":488100,"flag":"https://restcountries.eu/data/tkm.svg","code":"TM","name":"Turkmenistan","density":9.7},{"capital":"Funafuti","population":10640,"area":26,"flag":"https://restcountries.eu/data/tuv.svg","code":"TV","name":"Tuvalu","density":409.2},{"capital":"Kampala","population":33860700,"area":241550,"flag":"https://restcountries.eu/data/uga.svg","code":"UG","name":"Uganda","density":140.2},{"capital":"Kiev","population":42692393,"area":603700,"flag":"https://restcountries.eu/data/ukr.svg","code":"UA","name":"Ukraine","density":70.7},{"capital":"Montevideo","population":3480222,"area":181034,"flag":"https://restcountries.eu/data/ury.svg","code":"UY","name":"Uruguay","density":19.2},{"capital":"Tashkent","population":31576400,"area":447400,"flag":"https://restcountries.eu/data/uzb.svg","code":"UZ","name":"Uzbekistan","density":70.6},{"capital":"Port Vila","population":277500,"area":12189,"flag":"https://restcountries.eu/data/vut.svg","code":"VU","name":"Vanuatu","density":22.8},{"capital":"Hanoi","population":92700000,"area":331212,"flag":"https://restcountries.eu/data/vnm.svg","code":"VN","name":"Viet Nam","density":279.9},{"capital":"El Aaiún","population":510713,"area":266000,"flag":"https://restcountries.eu/data/esh.svg","code":"EH","name":"Western Sahara","density":1.9},{"capital":"Sana'a","population":27478000,"area":527968,"flag":"https://restcountries.eu/data/yem.svg","code":"YE","name":"Yemen","density":52},{"capital":"Lusaka","population":15933883,"area":752612,"flag":"https://restcountries.eu/data/zmb.svg","code":"ZM","name":"Zambia","density":21.2},{"capital":"Harare","population":14240168,"area":390757,"flag":"https://restcountries.eu/data/zwe.svg","code":"ZW","name":"Zimbabwe","density":36.4}]
},{}],2:[function(require,module,exports){
'use strict'

const countries = require('./countries.json')
const shuffle = require('lodash.shuffle')
const numberFormatter = require('number-formatter')

// const toGermanNo = (e) => numberFormatter('#.##0,##', e)
const toEnglishNo = (e) => numberFormatter('#,##0.##', e)

let currentSet
let locked = false
let highscore = 0

const categories = {
	'population': 'Population',
	'density': 'Density',
	'area': 'Area'
}

const units = {
	'population': '',
	'density': ' per km²',
	'area': ' km²'
}

const setSign = (sign) => document.querySelector("#sign").innerHTML = sign
const setCategory = (category) => document.querySelector("#category").innerHTML = categories[category]
const setHighscore = (hs) => document.querySelector("#highscore").innerHTML = hs
const setClickToContinue = (text) => document.querySelector('#continue').innerHTML = text

const setColorClass = (c) => {
	document.querySelectorAll(".fact").forEach((e) => e.setAttribute('class', 'fact '+c))
	document.querySelector("#sign").setAttribute('class', c)
}

const setSide = (lr, items, category, fact) => {
	document.querySelector('#'+lr).querySelector('.flag').setAttribute("src", items.flag)
	document.querySelector('#'+lr).querySelector('.name').innerHTML = items.name
	if(fact) document.querySelector('#'+lr).querySelector('.fact').innerHTML = toEnglishNo(items.value) + units[category]
	else document.querySelector('#'+lr).querySelector('.fact').innerHTML = '?'
}

const generateSet = () => {
	const shuffled = shuffle(countries)
	const left = shuffled[0]
	const right = shuffled[1]

	const category = shuffle(Object.keys(categories))[0]

	let correct
	if(left[category] >= right[category]) correct = 'left'
	else correct = 'right'

	return ({
		items: {
			left: {
				name: left.name,
				flag: left.flag,
				value: left[category]
			},
			right: {
				name: right.name,
				flag: right.flag,
				value: right[category]
			}
		},
		category: category,
		correct
	})
}

const reset = () => {
	locked = false
	document.querySelector('body').style.cursor = 'initial'
	setHighscore(highscore)
	setColorClass('normal')
	currentSet = generateSet()
	setCategory(currentSet.category)
	setSide("left", currentSet.items.left, currentSet.category, false)
	setSide("right", currentSet.items.right, currentSet.category, false)
	setClickToContinue('')
}

const result = (lr) => {
	locked = true
	document.querySelector('body').style.cursor = 'pointer'
	if(lr === currentSet.correct){
		setColorClass('right')
		highscore++
		setHighscore(highscore)
	}
	else{
		setColorClass('wrong')
		setHighscore(highscore)
		highscore = 0
	}
	setSide("left", currentSet.items.left, currentSet.category, true)
	setSide("right", currentSet.items.right, currentSet.category, true)
	setClickToContinue('Click to continue')
}

reset()

document.querySelector("#left").addEventListener('mouseover', (e) => !locked ? setSign('>') : null)
document.querySelector("#right").addEventListener('mouseover', (e) => !locked ? setSign('<') : null)

document.querySelector("#left").addEventListener('click', (e) => {
	if(!locked){
		e.stopPropagation()
		result("left")
	}
})
document.querySelector("#right").addEventListener('click', (e) => {
	if(!locked){
		e.stopPropagation()
		result("right")
	}
})

document.querySelector('body').addEventListener('click', () => locked ? reset() : null)

},{"./countries.json":1,"lodash.shuffle":3,"number-formatter":4}],3:[function(require,module,exports){
(function (global){
/**
 * lodash (Custom Build) <https://lodash.com/>
 * Build: `lodash modularize exports="npm" -o ./`
 * Copyright jQuery Foundation and other contributors <https://jquery.org/>
 * Released under MIT license <https://lodash.com/license>
 * Based on Underscore.js 1.8.3 <http://underscorejs.org/LICENSE>
 * Copyright Jeremy Ashkenas, DocumentCloud and Investigative Reporters & Editors
 */

/** Used as references for various `Number` constants. */
var INFINITY = 1 / 0,
    MAX_SAFE_INTEGER = 9007199254740991,
    MAX_INTEGER = 1.7976931348623157e+308,
    NAN = 0 / 0;

/** Used as references for the maximum length and index of an array. */
var MAX_ARRAY_LENGTH = 4294967295;

/** `Object#toString` result references. */
var argsTag = '[object Arguments]',
    funcTag = '[object Function]',
    genTag = '[object GeneratorFunction]',
    mapTag = '[object Map]',
    objectTag = '[object Object]',
    promiseTag = '[object Promise]',
    setTag = '[object Set]',
    stringTag = '[object String]',
    symbolTag = '[object Symbol]',
    weakMapTag = '[object WeakMap]';

var dataViewTag = '[object DataView]';

/**
 * Used to match `RegExp`
 * [syntax characters](http://ecma-international.org/ecma-262/7.0/#sec-patterns).
 */
var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;

/** Used to match leading and trailing whitespace. */
var reTrim = /^\s+|\s+$/g;

/** Used to detect bad signed hexadecimal string values. */
var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;

/** Used to detect binary string values. */
var reIsBinary = /^0b[01]+$/i;

/** Used to detect host constructors (Safari). */
var reIsHostCtor = /^\[object .+?Constructor\]$/;

/** Used to detect octal string values. */
var reIsOctal = /^0o[0-7]+$/i;

/** Used to detect unsigned integer values. */
var reIsUint = /^(?:0|[1-9]\d*)$/;

/** Used to compose unicode character classes. */
var rsAstralRange = '\\ud800-\\udfff',
    rsComboMarksRange = '\\u0300-\\u036f\\ufe20-\\ufe23',
    rsComboSymbolsRange = '\\u20d0-\\u20f0',
    rsVarRange = '\\ufe0e\\ufe0f';

/** Used to compose unicode capture groups. */
var rsAstral = '[' + rsAstralRange + ']',
    rsCombo = '[' + rsComboMarksRange + rsComboSymbolsRange + ']',
    rsFitz = '\\ud83c[\\udffb-\\udfff]',
    rsModifier = '(?:' + rsCombo + '|' + rsFitz + ')',
    rsNonAstral = '[^' + rsAstralRange + ']',
    rsRegional = '(?:\\ud83c[\\udde6-\\uddff]){2}',
    rsSurrPair = '[\\ud800-\\udbff][\\udc00-\\udfff]',
    rsZWJ = '\\u200d';

/** Used to compose unicode regexes. */
var reOptMod = rsModifier + '?',
    rsOptVar = '[' + rsVarRange + ']?',
    rsOptJoin = '(?:' + rsZWJ + '(?:' + [rsNonAstral, rsRegional, rsSurrPair].join('|') + ')' + rsOptVar + reOptMod + ')*',
    rsSeq = rsOptVar + reOptMod + rsOptJoin,
    rsSymbol = '(?:' + [rsNonAstral + rsCombo + '?', rsCombo, rsRegional, rsSurrPair, rsAstral].join('|') + ')';

/** Used to match [string symbols](https://mathiasbynens.be/notes/javascript-unicode). */
var reUnicode = RegExp(rsFitz + '(?=' + rsFitz + ')|' + rsSymbol + rsSeq, 'g');

/** Used to detect strings with [zero-width joiners or code points from the astral planes](http://eev.ee/blog/2015/09/12/dark-corners-of-unicode/). */
var reHasUnicode = RegExp('[' + rsZWJ + rsAstralRange  + rsComboMarksRange + rsComboSymbolsRange + rsVarRange + ']');

/** Built-in method references without a dependency on `root`. */
var freeParseInt = parseInt;

/** Detect free variable `global` from Node.js. */
var freeGlobal = typeof global == 'object' && global && global.Object === Object && global;

/** Detect free variable `self`. */
var freeSelf = typeof self == 'object' && self && self.Object === Object && self;

/** Used as a reference to the global object. */
var root = freeGlobal || freeSelf || Function('return this')();

/**
 * A specialized version of `_.map` for arrays without support for iteratee
 * shorthands.
 *
 * @private
 * @param {Array} [array] The array to iterate over.
 * @param {Function} iteratee The function invoked per iteration.
 * @returns {Array} Returns the new mapped array.
 */
function arrayMap(array, iteratee) {
  var index = -1,
      length = array ? array.length : 0,
      result = Array(length);

  while (++index < length) {
    result[index] = iteratee(array[index], index, array);
  }
  return result;
}

/**
 * Converts an ASCII `string` to an array.
 *
 * @private
 * @param {string} string The string to convert.
 * @returns {Array} Returns the converted array.
 */
function asciiToArray(string) {
  return string.split('');
}

/**
 * The base implementation of `_.times` without support for iteratee shorthands
 * or max array length checks.
 *
 * @private
 * @param {number} n The number of times to invoke `iteratee`.
 * @param {Function} iteratee The function invoked per iteration.
 * @returns {Array} Returns the array of results.
 */
function baseTimes(n, iteratee) {
  var index = -1,
      result = Array(n);

  while (++index < n) {
    result[index] = iteratee(index);
  }
  return result;
}

/**
 * The base implementation of `_.values` and `_.valuesIn` which creates an
 * array of `object` property values corresponding to the property names
 * of `props`.
 *
 * @private
 * @param {Object} object The object to query.
 * @param {Array} props The property names to get values for.
 * @returns {Object} Returns the array of property values.
 */
function baseValues(object, props) {
  return arrayMap(props, function(key) {
    return object[key];
  });
}

/**
 * Gets the value at `key` of `object`.
 *
 * @private
 * @param {Object} [object] The object to query.
 * @param {string} key The key of the property to get.
 * @returns {*} Returns the property value.
 */
function getValue(object, key) {
  return object == null ? undefined : object[key];
}

/**
 * Checks if `string` contains Unicode symbols.
 *
 * @private
 * @param {string} string The string to inspect.
 * @returns {boolean} Returns `true` if a symbol is found, else `false`.
 */
function hasUnicode(string) {
  return reHasUnicode.test(string);
}

/**
 * Checks if `value` is a host object in IE < 9.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a host object, else `false`.
 */
function isHostObject(value) {
  // Many host objects are `Object` objects that can coerce to strings
  // despite having improperly defined `toString` methods.
  var result = false;
  if (value != null && typeof value.toString != 'function') {
    try {
      result = !!(value + '');
    } catch (e) {}
  }
  return result;
}

/**
 * Converts `iterator` to an array.
 *
 * @private
 * @param {Object} iterator The iterator to convert.
 * @returns {Array} Returns the converted array.
 */
function iteratorToArray(iterator) {
  var data,
      result = [];

  while (!(data = iterator.next()).done) {
    result.push(data.value);
  }
  return result;
}

/**
 * Converts `map` to its key-value pairs.
 *
 * @private
 * @param {Object} map The map to convert.
 * @returns {Array} Returns the key-value pairs.
 */
function mapToArray(map) {
  var index = -1,
      result = Array(map.size);

  map.forEach(function(value, key) {
    result[++index] = [key, value];
  });
  return result;
}

/**
 * Creates a unary function that invokes `func` with its argument transformed.
 *
 * @private
 * @param {Function} func The function to wrap.
 * @param {Function} transform The argument transform.
 * @returns {Function} Returns the new function.
 */
function overArg(func, transform) {
  return function(arg) {
    return func(transform(arg));
  };
}

/**
 * Converts `set` to an array of its values.
 *
 * @private
 * @param {Object} set The set to convert.
 * @returns {Array} Returns the values.
 */
function setToArray(set) {
  var index = -1,
      result = Array(set.size);

  set.forEach(function(value) {
    result[++index] = value;
  });
  return result;
}

/**
 * Converts `string` to an array.
 *
 * @private
 * @param {string} string The string to convert.
 * @returns {Array} Returns the converted array.
 */
function stringToArray(string) {
  return hasUnicode(string)
    ? unicodeToArray(string)
    : asciiToArray(string);
}

/**
 * Converts a Unicode `string` to an array.
 *
 * @private
 * @param {string} string The string to convert.
 * @returns {Array} Returns the converted array.
 */
function unicodeToArray(string) {
  return string.match(reUnicode) || [];
}

/** Used for built-in method references. */
var funcProto = Function.prototype,
    objectProto = Object.prototype;

/** Used to detect overreaching core-js shims. */
var coreJsData = root['__core-js_shared__'];

/** Used to detect methods masquerading as native. */
var maskSrcKey = (function() {
  var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || '');
  return uid ? ('Symbol(src)_1.' + uid) : '';
}());

/** Used to resolve the decompiled source of functions. */
var funcToString = funcProto.toString;

/** Used to check objects for own properties. */
var hasOwnProperty = objectProto.hasOwnProperty;

/**
 * Used to resolve the
 * [`toStringTag`](http://ecma-international.org/ecma-262/7.0/#sec-object.prototype.tostring)
 * of values.
 */
var objectToString = objectProto.toString;

/** Used to detect if a method is native. */
var reIsNative = RegExp('^' +
  funcToString.call(hasOwnProperty).replace(reRegExpChar, '\\$&')
  .replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, '$1.*?') + '$'
);

/** Built-in value references. */
var Symbol = root.Symbol,
    iteratorSymbol = Symbol ? Symbol.iterator : undefined,
    propertyIsEnumerable = objectProto.propertyIsEnumerable;

/* Built-in method references for those with the same name as other `lodash` methods. */
var nativeFloor = Math.floor,
    nativeKeys = overArg(Object.keys, Object),
    nativeRandom = Math.random;

/* Built-in method references that are verified to be native. */
var DataView = getNative(root, 'DataView'),
    Map = getNative(root, 'Map'),
    Promise = getNative(root, 'Promise'),
    Set = getNative(root, 'Set'),
    WeakMap = getNative(root, 'WeakMap');

/** Used to detect maps, sets, and weakmaps. */
var dataViewCtorString = toSource(DataView),
    mapCtorString = toSource(Map),
    promiseCtorString = toSource(Promise),
    setCtorString = toSource(Set),
    weakMapCtorString = toSource(WeakMap);

/**
 * Creates an array of the enumerable property names of the array-like `value`.
 *
 * @private
 * @param {*} value The value to query.
 * @param {boolean} inherited Specify returning inherited property names.
 * @returns {Array} Returns the array of property names.
 */
function arrayLikeKeys(value, inherited) {
  // Safari 8.1 makes `arguments.callee` enumerable in strict mode.
  // Safari 9 makes `arguments.length` enumerable in strict mode.
  var result = (isArray(value) || isArguments(value))
    ? baseTimes(value.length, String)
    : [];

  var length = result.length,
      skipIndexes = !!length;

  for (var key in value) {
    if ((inherited || hasOwnProperty.call(value, key)) &&
        !(skipIndexes && (key == 'length' || isIndex(key, length)))) {
      result.push(key);
    }
  }
  return result;
}

/**
 * The base implementation of `_.clamp` which doesn't coerce arguments.
 *
 * @private
 * @param {number} number The number to clamp.
 * @param {number} [lower] The lower bound.
 * @param {number} upper The upper bound.
 * @returns {number} Returns the clamped number.
 */
function baseClamp(number, lower, upper) {
  if (number === number) {
    if (upper !== undefined) {
      number = number <= upper ? number : upper;
    }
    if (lower !== undefined) {
      number = number >= lower ? number : lower;
    }
  }
  return number;
}

/**
 * The base implementation of `getTag`.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the `toStringTag`.
 */
function baseGetTag(value) {
  return objectToString.call(value);
}

/**
 * The base implementation of `_.isNative` without bad shim checks.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a native function,
 *  else `false`.
 */
function baseIsNative(value) {
  if (!isObject(value) || isMasked(value)) {
    return false;
  }
  var pattern = (isFunction(value) || isHostObject(value)) ? reIsNative : reIsHostCtor;
  return pattern.test(toSource(value));
}

/**
 * The base implementation of `_.keys` which doesn't treat sparse arrays as dense.
 *
 * @private
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names.
 */
function baseKeys(object) {
  if (!isPrototype(object)) {
    return nativeKeys(object);
  }
  var result = [];
  for (var key in Object(object)) {
    if (hasOwnProperty.call(object, key) && key != 'constructor') {
      result.push(key);
    }
  }
  return result;
}

/**
 * The base implementation of `_.random` without support for returning
 * floating-point numbers.
 *
 * @private
 * @param {number} lower The lower bound.
 * @param {number} upper The upper bound.
 * @returns {number} Returns the random number.
 */
function baseRandom(lower, upper) {
  return lower + nativeFloor(nativeRandom() * (upper - lower + 1));
}

/**
 * Copies the values of `source` to `array`.
 *
 * @private
 * @param {Array} source The array to copy values from.
 * @param {Array} [array=[]] The array to copy values to.
 * @returns {Array} Returns `array`.
 */
function copyArray(source, array) {
  var index = -1,
      length = source.length;

  array || (array = Array(length));
  while (++index < length) {
    array[index] = source[index];
  }
  return array;
}

/**
 * Gets the native function at `key` of `object`.
 *
 * @private
 * @param {Object} object The object to query.
 * @param {string} key The key of the method to get.
 * @returns {*} Returns the function if it's native, else `undefined`.
 */
function getNative(object, key) {
  var value = getValue(object, key);
  return baseIsNative(value) ? value : undefined;
}

/**
 * Gets the `toStringTag` of `value`.
 *
 * @private
 * @param {*} value The value to query.
 * @returns {string} Returns the `toStringTag`.
 */
var getTag = baseGetTag;

// Fallback for data views, maps, sets, and weak maps in IE 11,
// for data views in Edge < 14, and promises in Node.js.
if ((DataView && getTag(new DataView(new ArrayBuffer(1))) != dataViewTag) ||
    (Map && getTag(new Map) != mapTag) ||
    (Promise && getTag(Promise.resolve()) != promiseTag) ||
    (Set && getTag(new Set) != setTag) ||
    (WeakMap && getTag(new WeakMap) != weakMapTag)) {
  getTag = function(value) {
    var result = objectToString.call(value),
        Ctor = result == objectTag ? value.constructor : undefined,
        ctorString = Ctor ? toSource(Ctor) : undefined;

    if (ctorString) {
      switch (ctorString) {
        case dataViewCtorString: return dataViewTag;
        case mapCtorString: return mapTag;
        case promiseCtorString: return promiseTag;
        case setCtorString: return setTag;
        case weakMapCtorString: return weakMapTag;
      }
    }
    return result;
  };
}

/**
 * Checks if `value` is a valid array-like index.
 *
 * @private
 * @param {*} value The value to check.
 * @param {number} [length=MAX_SAFE_INTEGER] The upper bounds of a valid index.
 * @returns {boolean} Returns `true` if `value` is a valid index, else `false`.
 */
function isIndex(value, length) {
  length = length == null ? MAX_SAFE_INTEGER : length;
  return !!length &&
    (typeof value == 'number' || reIsUint.test(value)) &&
    (value > -1 && value % 1 == 0 && value < length);
}

/**
 * Checks if the given arguments are from an iteratee call.
 *
 * @private
 * @param {*} value The potential iteratee value argument.
 * @param {*} index The potential iteratee index or key argument.
 * @param {*} object The potential iteratee object argument.
 * @returns {boolean} Returns `true` if the arguments are from an iteratee call,
 *  else `false`.
 */
function isIterateeCall(value, index, object) {
  if (!isObject(object)) {
    return false;
  }
  var type = typeof index;
  if (type == 'number'
        ? (isArrayLike(object) && isIndex(index, object.length))
        : (type == 'string' && index in object)
      ) {
    return eq(object[index], value);
  }
  return false;
}

/**
 * Checks if `func` has its source masked.
 *
 * @private
 * @param {Function} func The function to check.
 * @returns {boolean} Returns `true` if `func` is masked, else `false`.
 */
function isMasked(func) {
  return !!maskSrcKey && (maskSrcKey in func);
}

/**
 * Checks if `value` is likely a prototype object.
 *
 * @private
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a prototype, else `false`.
 */
function isPrototype(value) {
  var Ctor = value && value.constructor,
      proto = (typeof Ctor == 'function' && Ctor.prototype) || objectProto;

  return value === proto;
}

/**
 * Converts `func` to its source code.
 *
 * @private
 * @param {Function} func The function to process.
 * @returns {string} Returns the source code.
 */
function toSource(func) {
  if (func != null) {
    try {
      return funcToString.call(func);
    } catch (e) {}
    try {
      return (func + '');
    } catch (e) {}
  }
  return '';
}

/**
 * Gets `n` random elements at unique keys from `collection` up to the
 * size of `collection`.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Collection
 * @param {Array|Object} collection The collection to sample.
 * @param {number} [n=1] The number of elements to sample.
 * @param- {Object} [guard] Enables use as an iteratee for methods like `_.map`.
 * @returns {Array} Returns the random elements.
 * @example
 *
 * _.sampleSize([1, 2, 3], 2);
 * // => [3, 1]
 *
 * _.sampleSize([1, 2, 3], 4);
 * // => [2, 3, 1]
 */
function sampleSize(collection, n, guard) {
  var index = -1,
      result = toArray(collection),
      length = result.length,
      lastIndex = length - 1;

  if ((guard ? isIterateeCall(collection, n, guard) : n === undefined)) {
    n = 1;
  } else {
    n = baseClamp(toInteger(n), 0, length);
  }
  while (++index < n) {
    var rand = baseRandom(index, lastIndex),
        value = result[rand];

    result[rand] = result[index];
    result[index] = value;
  }
  result.length = n;
  return result;
}

/**
 * Creates an array of shuffled values, using a version of the
 * [Fisher-Yates shuffle](https://en.wikipedia.org/wiki/Fisher-Yates_shuffle).
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Collection
 * @param {Array|Object} collection The collection to shuffle.
 * @returns {Array} Returns the new shuffled array.
 * @example
 *
 * _.shuffle([1, 2, 3, 4]);
 * // => [4, 1, 3, 2]
 */
function shuffle(collection) {
  return sampleSize(collection, MAX_ARRAY_LENGTH);
}

/**
 * Performs a
 * [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)
 * comparison between two values to determine if they are equivalent.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to compare.
 * @param {*} other The other value to compare.
 * @returns {boolean} Returns `true` if the values are equivalent, else `false`.
 * @example
 *
 * var object = { 'a': 1 };
 * var other = { 'a': 1 };
 *
 * _.eq(object, object);
 * // => true
 *
 * _.eq(object, other);
 * // => false
 *
 * _.eq('a', 'a');
 * // => true
 *
 * _.eq('a', Object('a'));
 * // => false
 *
 * _.eq(NaN, NaN);
 * // => true
 */
function eq(value, other) {
  return value === other || (value !== value && other !== other);
}

/**
 * Checks if `value` is likely an `arguments` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an `arguments` object,
 *  else `false`.
 * @example
 *
 * _.isArguments(function() { return arguments; }());
 * // => true
 *
 * _.isArguments([1, 2, 3]);
 * // => false
 */
function isArguments(value) {
  // Safari 8.1 makes `arguments.callee` enumerable in strict mode.
  return isArrayLikeObject(value) && hasOwnProperty.call(value, 'callee') &&
    (!propertyIsEnumerable.call(value, 'callee') || objectToString.call(value) == argsTag);
}

/**
 * Checks if `value` is classified as an `Array` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an array, else `false`.
 * @example
 *
 * _.isArray([1, 2, 3]);
 * // => true
 *
 * _.isArray(document.body.children);
 * // => false
 *
 * _.isArray('abc');
 * // => false
 *
 * _.isArray(_.noop);
 * // => false
 */
var isArray = Array.isArray;

/**
 * Checks if `value` is array-like. A value is considered array-like if it's
 * not a function and has a `value.length` that's an integer greater than or
 * equal to `0` and less than or equal to `Number.MAX_SAFE_INTEGER`.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is array-like, else `false`.
 * @example
 *
 * _.isArrayLike([1, 2, 3]);
 * // => true
 *
 * _.isArrayLike(document.body.children);
 * // => true
 *
 * _.isArrayLike('abc');
 * // => true
 *
 * _.isArrayLike(_.noop);
 * // => false
 */
function isArrayLike(value) {
  return value != null && isLength(value.length) && !isFunction(value);
}

/**
 * This method is like `_.isArrayLike` except that it also checks if `value`
 * is an object.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an array-like object,
 *  else `false`.
 * @example
 *
 * _.isArrayLikeObject([1, 2, 3]);
 * // => true
 *
 * _.isArrayLikeObject(document.body.children);
 * // => true
 *
 * _.isArrayLikeObject('abc');
 * // => false
 *
 * _.isArrayLikeObject(_.noop);
 * // => false
 */
function isArrayLikeObject(value) {
  return isObjectLike(value) && isArrayLike(value);
}

/**
 * Checks if `value` is classified as a `Function` object.
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a function, else `false`.
 * @example
 *
 * _.isFunction(_);
 * // => true
 *
 * _.isFunction(/abc/);
 * // => false
 */
function isFunction(value) {
  // The use of `Object#toString` avoids issues with the `typeof` operator
  // in Safari 8-9 which returns 'object' for typed array and other constructors.
  var tag = isObject(value) ? objectToString.call(value) : '';
  return tag == funcTag || tag == genTag;
}

/**
 * Checks if `value` is a valid array-like length.
 *
 * **Note:** This method is loosely based on
 * [`ToLength`](http://ecma-international.org/ecma-262/7.0/#sec-tolength).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a valid length, else `false`.
 * @example
 *
 * _.isLength(3);
 * // => true
 *
 * _.isLength(Number.MIN_VALUE);
 * // => false
 *
 * _.isLength(Infinity);
 * // => false
 *
 * _.isLength('3');
 * // => false
 */
function isLength(value) {
  return typeof value == 'number' &&
    value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
}

/**
 * Checks if `value` is the
 * [language type](http://www.ecma-international.org/ecma-262/7.0/#sec-ecmascript-language-types)
 * of `Object`. (e.g. arrays, functions, objects, regexes, `new Number(0)`, and `new String('')`)
 *
 * @static
 * @memberOf _
 * @since 0.1.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is an object, else `false`.
 * @example
 *
 * _.isObject({});
 * // => true
 *
 * _.isObject([1, 2, 3]);
 * // => true
 *
 * _.isObject(_.noop);
 * // => true
 *
 * _.isObject(null);
 * // => false
 */
function isObject(value) {
  var type = typeof value;
  return !!value && (type == 'object' || type == 'function');
}

/**
 * Checks if `value` is object-like. A value is object-like if it's not `null`
 * and has a `typeof` result of "object".
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is object-like, else `false`.
 * @example
 *
 * _.isObjectLike({});
 * // => true
 *
 * _.isObjectLike([1, 2, 3]);
 * // => true
 *
 * _.isObjectLike(_.noop);
 * // => false
 *
 * _.isObjectLike(null);
 * // => false
 */
function isObjectLike(value) {
  return !!value && typeof value == 'object';
}

/**
 * Checks if `value` is classified as a `String` primitive or object.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a string, else `false`.
 * @example
 *
 * _.isString('abc');
 * // => true
 *
 * _.isString(1);
 * // => false
 */
function isString(value) {
  return typeof value == 'string' ||
    (!isArray(value) && isObjectLike(value) && objectToString.call(value) == stringTag);
}

/**
 * Checks if `value` is classified as a `Symbol` primitive or object.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to check.
 * @returns {boolean} Returns `true` if `value` is a symbol, else `false`.
 * @example
 *
 * _.isSymbol(Symbol.iterator);
 * // => true
 *
 * _.isSymbol('abc');
 * // => false
 */
function isSymbol(value) {
  return typeof value == 'symbol' ||
    (isObjectLike(value) && objectToString.call(value) == symbolTag);
}

/**
 * Converts `value` to an array.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Lang
 * @param {*} value The value to convert.
 * @returns {Array} Returns the converted array.
 * @example
 *
 * _.toArray({ 'a': 1, 'b': 2 });
 * // => [1, 2]
 *
 * _.toArray('abc');
 * // => ['a', 'b', 'c']
 *
 * _.toArray(1);
 * // => []
 *
 * _.toArray(null);
 * // => []
 */
function toArray(value) {
  if (!value) {
    return [];
  }
  if (isArrayLike(value)) {
    return isString(value) ? stringToArray(value) : copyArray(value);
  }
  if (iteratorSymbol && value[iteratorSymbol]) {
    return iteratorToArray(value[iteratorSymbol]());
  }
  var tag = getTag(value),
      func = tag == mapTag ? mapToArray : (tag == setTag ? setToArray : values);

  return func(value);
}

/**
 * Converts `value` to a finite number.
 *
 * @static
 * @memberOf _
 * @since 4.12.0
 * @category Lang
 * @param {*} value The value to convert.
 * @returns {number} Returns the converted number.
 * @example
 *
 * _.toFinite(3.2);
 * // => 3.2
 *
 * _.toFinite(Number.MIN_VALUE);
 * // => 5e-324
 *
 * _.toFinite(Infinity);
 * // => 1.7976931348623157e+308
 *
 * _.toFinite('3.2');
 * // => 3.2
 */
function toFinite(value) {
  if (!value) {
    return value === 0 ? value : 0;
  }
  value = toNumber(value);
  if (value === INFINITY || value === -INFINITY) {
    var sign = (value < 0 ? -1 : 1);
    return sign * MAX_INTEGER;
  }
  return value === value ? value : 0;
}

/**
 * Converts `value` to an integer.
 *
 * **Note:** This method is loosely based on
 * [`ToInteger`](http://www.ecma-international.org/ecma-262/7.0/#sec-tointeger).
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to convert.
 * @returns {number} Returns the converted integer.
 * @example
 *
 * _.toInteger(3.2);
 * // => 3
 *
 * _.toInteger(Number.MIN_VALUE);
 * // => 0
 *
 * _.toInteger(Infinity);
 * // => 1.7976931348623157e+308
 *
 * _.toInteger('3.2');
 * // => 3
 */
function toInteger(value) {
  var result = toFinite(value),
      remainder = result % 1;

  return result === result ? (remainder ? result - remainder : result) : 0;
}

/**
 * Converts `value` to a number.
 *
 * @static
 * @memberOf _
 * @since 4.0.0
 * @category Lang
 * @param {*} value The value to process.
 * @returns {number} Returns the number.
 * @example
 *
 * _.toNumber(3.2);
 * // => 3.2
 *
 * _.toNumber(Number.MIN_VALUE);
 * // => 5e-324
 *
 * _.toNumber(Infinity);
 * // => Infinity
 *
 * _.toNumber('3.2');
 * // => 3.2
 */
function toNumber(value) {
  if (typeof value == 'number') {
    return value;
  }
  if (isSymbol(value)) {
    return NAN;
  }
  if (isObject(value)) {
    var other = typeof value.valueOf == 'function' ? value.valueOf() : value;
    value = isObject(other) ? (other + '') : other;
  }
  if (typeof value != 'string') {
    return value === 0 ? value : +value;
  }
  value = value.replace(reTrim, '');
  var isBinary = reIsBinary.test(value);
  return (isBinary || reIsOctal.test(value))
    ? freeParseInt(value.slice(2), isBinary ? 2 : 8)
    : (reIsBadHex.test(value) ? NAN : +value);
}

/**
 * Creates an array of the own enumerable property names of `object`.
 *
 * **Note:** Non-object values are coerced to objects. See the
 * [ES spec](http://ecma-international.org/ecma-262/7.0/#sec-object.keys)
 * for more details.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Object
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property names.
 * @example
 *
 * function Foo() {
 *   this.a = 1;
 *   this.b = 2;
 * }
 *
 * Foo.prototype.c = 3;
 *
 * _.keys(new Foo);
 * // => ['a', 'b'] (iteration order is not guaranteed)
 *
 * _.keys('hi');
 * // => ['0', '1']
 */
function keys(object) {
  return isArrayLike(object) ? arrayLikeKeys(object) : baseKeys(object);
}

/**
 * Creates an array of the own enumerable string keyed property values of `object`.
 *
 * **Note:** Non-object values are coerced to objects.
 *
 * @static
 * @since 0.1.0
 * @memberOf _
 * @category Object
 * @param {Object} object The object to query.
 * @returns {Array} Returns the array of property values.
 * @example
 *
 * function Foo() {
 *   this.a = 1;
 *   this.b = 2;
 * }
 *
 * Foo.prototype.c = 3;
 *
 * _.values(new Foo);
 * // => [1, 2] (iteration order is not guaranteed)
 *
 * _.values('hi');
 * // => ['h', 'i']
 */
function values(object) {
  return object ? baseValues(object, keys(object)) : [];
}

module.exports = shuffle;

}).call(this,typeof global !== "undefined" ? global : typeof self !== "undefined" ? self : typeof window !== "undefined" ? window : {})
},{}],4:[function(require,module,exports){
/**
 * number-formatter
 * Lightweight & Fast JavaScript Number Formatter
 *
 * @preserve IntegraXor Web SCADA - JavaScript Number Formatter (http://www.integraxor.com/)
 * @author KPL
 * @maintainer Rob Garrison
 * @copyright 2015 ecava
 * @license MIT <http://www.opensource.org/licenses/mit-license.php>
 * @link http://mottie.github.com/javascript-number-formatter/
 * @version 1.1.8
 */
/* global define, module */
(function ( root, factory ) {
	if ( typeof define === 'function' ) {
		define( 'number-formatter', ['exports'], function (exports) {
			return (exports['default'] = factory());
		});
	} else if ( typeof module === 'object' ) {
		module.exports = factory();
	} else {
		root.numberFormatter = factory();
	}
}( this, function () {

	return function ( mask, value ) {
		'use strict';
		if ( !mask || isNaN( +value ) ) {
			return value; // return as it is.
		}

		var isNegative, result, decimal, group, posLeadZero, posTrailZero, posSeparator,
			part, szSep, integer,

			// find prefix/suffix
			len = mask.length,
			start = mask.search( /[0-9\-\+#]/ ),
			prefix = start > 0 ? mask.substring( 0, start ) : '',
			// reverse string: not an ideal method if there are surrogate pairs
			str = mask.split( '' ).reverse().join( '' ),
			end = str.search( /[0-9\-\+#]/ ),
			offset = len - end,
			indx = offset + ( ( mask.substring( offset, offset + 1 ) === '.' ) ? 1 : 0 ),
			suffix = end > 0 ? mask.substring( indx, len ) : '';

		// mask with prefix & suffix removed
		mask = mask.substring( start, indx );

		// convert any string to number according to formation sign.
		value = mask.charAt( 0 ) === '-' ? -value : +value;
		isNegative = value < 0 ? value = -value : 0; // process only abs(), and turn on flag.

		// search for separator for grp & decimal, anything not digit, not +/- sign, not #.
		result = mask.match( /[^\d\-\+#]/g );
		decimal = ( result && result[ result.length - 1 ] ) || '.'; // treat the right most symbol as decimal
		group = ( result && result[ 1 ] && result[ 0 ] ) || ',';  // treat the left most symbol as group separator

		// split the decimal for the format string if any.
		mask = mask.split( decimal );
		// Fix the decimal first, toFixed will auto fill trailing zero.
		value = value.toFixed( mask[ 1 ] && mask[ 1 ].length );
		value = +( value ) + ''; // convert number to string to trim off *all* trailing decimal zero(es)

		// fill back any trailing zero according to format
		posTrailZero = mask[ 1 ] && mask[ 1 ].lastIndexOf( '0' ); // look for last zero in format
		part = value.split( '.' );
		// integer will get !part[1]
		if ( !part[ 1 ] || ( part[ 1 ] && part[ 1 ].length <= posTrailZero ) ) {
			value = ( +value ).toFixed( posTrailZero + 1 );
		}
		szSep = mask[ 0 ].split( group ); // look for separator
		mask[ 0 ] = szSep.join( '' ); // join back without separator for counting the pos of any leading 0.

		posLeadZero = mask[ 0 ] && mask[ 0 ].indexOf( '0' );
		if ( posLeadZero > -1 ) {
			while ( part[ 0 ].length < ( mask[ 0 ].length - posLeadZero ) ) {
				part[ 0 ] = '0' + part[ 0 ];
			}
		} else if ( +part[ 0 ] === 0 ) {
			part[ 0 ] = '';
		}

		value = value.split( '.' );
		value[ 0 ] = part[ 0 ];

		// process the first group separator from decimal (.) only, the rest ignore.
		// get the length of the last slice of split result.
		posSeparator = ( szSep[ 1 ] && szSep[ szSep.length - 1 ].length );
		if ( posSeparator ) {
			integer = value[ 0 ];
			str = '';
			offset = integer.length % posSeparator;
			len = integer.length;
			for ( indx = 0; indx < len; indx++ ) {
				str += integer.charAt( indx ); // ie6 only support charAt for sz.
				// -posSeparator so that won't trail separator on full length
				/*jshint -W018 */
				if ( !( ( indx - offset + 1 ) % posSeparator ) && indx < len - posSeparator ) {
					str += group;
				}
			}
			value[ 0 ] = str;
		}
		value[ 1 ] = ( mask[ 1 ] && value[ 1 ] ) ? decimal + value[ 1 ] : '';

		// remove negative sign if result is zero
		result = value.join( '' );
		if ( result === '0' || result === '' ) {
			// remove negative sign if result is zero
			isNegative = false;
		}

		// put back any negation, combine integer and fraction, and add back prefix & suffix
		return prefix + ( ( isNegative ? '-' : '' ) + result ) + suffix;
	};

} ));

},{}]},{},[2]);
